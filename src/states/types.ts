export type RootState = {
  currentUserData: {
    loading: boolean;
    data: any;
    valid: boolean;
  };
  pathwayComponentData: {
    loading: boolean;
    data: any;
    valid: boolean;
  };
  approvePathway: {
    loading: boolean;
    data: any;
    valid: boolean;
  };
};
